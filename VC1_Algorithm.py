import tkinter as tk
import random
colors = ["red", "blue", "black", "yellow"] #color of candy
def movesnake():
    global snake, position
    for i in range(len(snake)):
        snake = snake[i]
        # [x0, y0, x1, y1]

        #ronan










canvas.pack(expand=True, fill='both')
frame.pack(expand=True, fill='both')
windows.bind("<Right-click", ....)
windows.bind("<Left-click", ....)
windows.bind("<Top-click", ....)
windows.bind("<Buttom-click", ....)



# Display all


windows.mainloop()